int main()
{
    int *p; //dichiaro un puntatore ad intero
    p = (int*) malloc(5 * sizeof(int)); //la funzione malloc prende un parametro che è lo spazio in memoria dell'array //dimensione array * sizeof del tipo corretto 
}